import {HubConnection, LogLevel} from '@aspnet/signalr';


// const connection = HubConnection.connection()
//     .withUrl("/chatHub")
//     .configureLogging(LogLevel.Information)
//     .build();


export const websocket = {
//connection
};
